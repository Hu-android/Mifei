<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--在去掉标题栏-->
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Edittext_theme">
        <item name="android:background">@null</item>
        <item name="android:textColorHint">@color/hint_text_color</item>
        <item name="android:textSize">14sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="Text_Text_theme">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#FF333333</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">@dimen/Android_15dp</item>
        <item name="android:paddingRight">@dimen/Android_15dp</item>
        <item name="android:paddingTop">@dimen/Android_12dp</item>
        <item name="android:paddingBottom">@dimen/Android_12dp</item>
    </style>

    <!--RadioButton-->
    <style name="RadioButton_blue">
        <item name="android:button">@drawable/custom_radio_btn</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- 引导页-->
    <style name="FullScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

</resources>
